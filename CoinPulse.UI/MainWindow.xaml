<Window x:Class="CoinPulse.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        xmlns:local="clr-namespace:CoinPulse.UI"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"
        Title="CoinPulse Dashboard" Height="700" Width="1100"
        Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextPrimaryBrush}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:WidthConverter x:Key="WidthConverter"/>
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                
                                <TextBlock Text="Search coin..." 
                                           Margin="2,0,0,0"
                                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Logo -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,30,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⚡" FontSize="28" Margin="0,0,10,0"/>
                    <TextBlock Text="CoinPulse" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
                <TextBlock Text="Live Crypto Market Dashboard" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="45,-5,0,0"/>
            </StackPanel>
            
            <!-- Search Bar -->
            <TextBox Grid.Column="1" Width="300" HorizontalAlignment="Left" VerticalAlignment="Center"
                     Style="{StaticResource SearchTextBoxStyle}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

            <!-- Controls (Auto-Refresh & Refresh) -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <CheckBox Content="Auto-Refresh (20s)" 
                          IsChecked="{Binding IsAutoRefreshEnabled}"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          VerticalContentAlignment="Center"
                          Margin="0,0,20,0"
                          Cursor="Hand"/>

                <Button Command="{Binding LoadDataCommand}"
                        Padding="25,10" Background="#2196F3" Foreground="White" 
                        FontWeight="SemiBold" BorderThickness="0" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Opacity" Value="0.9"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Opacity" Value="0.7"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="↻" Margin="0,0,8,0" FontSize="16"/>
                        <TextBlock Text="REFRESH"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Coin List -->
        <ListView Grid.Row="1" ItemsSource="{Binding Coins}" 
                  Background="Transparent" BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  ItemContainerStyle="{StaticResource CoinListViewItemStyle}"
                  SelectedItem="{Binding SelectedCoin}"
                  MouseDoubleClick="ListView_MouseDoubleClick"> 
            
            <ListView.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource DarkScrollBarStyle}"/>
            </ListView.Resources>
            
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="12" Padding="15" 
                            Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ListView}, Converter={StaticResource WidthConverter}, ConverterParameter=30}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>  
                                <ColumnDefinition Width="2*"/>  
                                <ColumnDefinition Width="3*"/>  
                                <ColumnDefinition Width="1.5*"/> 
                                <ColumnDefinition Width="1.2*"/> 
                            </Grid.ColumnDefinitions>

                            <!-- Icon -->
                            <Border Width="40" Height="40" CornerRadius="20" Background="White" HorizontalAlignment="Left">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.2" ShadowDepth="2"/>
                                </Border.Effect>
                                <Image Source="{Binding ImageUrl}" Width="40" Height="40"/>
                            </Border>
                            
                            <!-- Name & Symbol -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                <TextBlock Text="{Binding Symbol}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" FontWeight="Bold"/>
                            </StackPanel>

                            <!-- Sparkline Chart -->
                            <lvc:CartesianChart Grid.Column="2" 
                                                Series="{Binding ChartSeries}"
                                                XAxes="{Binding XAxes}"
                                                YAxes="{Binding YAxes}"
                                                Height="50"
                                                Margin="10,0"
                                                IsHitTestVisible="False"/>

                            <!-- Price -->
                            <TextBlock Grid.Column="3" Text="{Binding CurrentPrice, StringFormat='${0:N2}'}" 
                                       FontSize="18" FontWeight="Medium" VerticalAlignment="Center" HorizontalAlignment="Right" 
                                       Margin="0,0,20,0" Foreground="{StaticResource TextPrimaryBrush}"/>

                            <!-- Change 24h -->
                            <Border Grid.Column="4" Background="{Binding PriceChangeBackground}" CornerRadius="6" Padding="8,4" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding PriceChangePercentage24H, StringFormat='{}{0:N2}%'}" 
                                           Foreground="{Binding PriceChangeColor}"
                                           FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Footer Status -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Right">
            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Center"/>
            <ProgressBar Width="100" Height="4" Margin="15,0,0,0" IsIndeterminate="True" 
                         Background="{StaticResource SurfaceLightBrush}" BorderThickness="0"
                         Foreground="{StaticResource SecondaryBrush}"
                         Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</Window>